{
  "name": "portfolio-proxy",
  "version": "1.0.0",
  "description": "Local proxy to allow embedding external pages/PDFs in iframes for local dev",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  }
}
